[tool.black]
line-length = 100
target-version = ["py311"]
extend-exclude = "notebooks|data|models|reports|\\.venv|\\.git|docker"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
extend-select = ["I"]
exclude = ["notebooks", "data", "models", "reports", ".venv", ".git", "docker"]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
pretty = true
exclude = "(^notebooks/|^data/|^models/|^reports/|^\\.venv/|^docker/)"
